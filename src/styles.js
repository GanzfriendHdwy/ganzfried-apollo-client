const styles = ({ spacing }) => ({
  container: {
    // maxWidth: 600,
    padding: spacing(2),
    fontFamily: "sans-serif",
    textAlign: "center",
    display: "flex",
    flexDirection: "column",
    alignItems: "center"
  }
});

export default styles;
